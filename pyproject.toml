[tool.ruff]
line-length = 110
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["UP042"]  # str+Enum kept for broad Python-version compatibility (no StrEnum requirement)

[tool.pytest.ini_options]
testpaths = ["tests"]
